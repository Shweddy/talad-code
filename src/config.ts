// ศูนย์กลางลิงก์ GitHub ของตลาด — เปลี่ยนที่นี่ที่เดียว ใช้ทั้งเว็บ
// เมื่อย้ายไป org (เช่น taladcode/content) แก้แค่บรรทัด REPO
export const REPO = 'Shweddy/talad-code';
export const REPO_URL = `https://github.com/${REPO}`;
export const SUBMIT_URL = `${REPO_URL}/issues/new/choose`;
